server:
  port: 9001
spring:
  thymeleaf:
    enabled: true
    cache: false
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/loto?serverTimezone=JST&useSSL=false&characterEncoding=UTF-8
    username: test_user
    password: test_user
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  messages:
    basename: messages
    encoding: UTF-8
